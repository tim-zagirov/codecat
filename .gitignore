.build/
dist/
.DS_Store
*.xcodeproj

# Elthen's sprite sheet is fetched at build time, never committed: the author's
# terms allow using the sprites in a project but forbid passing the assets
# themselves on, and a public repository does exactly that.
# See scripts/fetch-optional-assets.sh and THIRD_PARTY_NOTICES.md.
Sources/CodeCatApp/Skins/elthen/

# The author's own working notes. They are not documentation and they are not
# for users; they stay out of the repository entirely.
.superpowers/

# A working document the author generated but never committed; it describes the
# pre-0.3.0 state (Russian UI, 151 commits) and would publish as stale.
PRODUCT_CONTEXT.md
